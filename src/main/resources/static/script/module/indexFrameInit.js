define(function (require) {

    function init() {

    }


    function initializeAsTeacher() {
        init();
    }

    function initializeAsStudent() {
        init();
    }

    return{
        initializeAsTeacher:initializeAsTeacher,
        initializeAsStudent:initializeAsStudent
    }
})